## ADDED Requirements

### Requirement: healthier-swaps-display
The system SHALL support fetching healthier swaps and displaying them inline inside the recipe details view.

#### Scenario: User clicks "Make it healthier"
- **WHEN** the user is viewing a saved recipe and taps "Make it healthier"
- **THEN** the system SHALL fetch healthier swaps and display the alternatives underneath the corresponding ingredient lines.
